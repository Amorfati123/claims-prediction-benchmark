# Pin these to the versions you actually ran before archiving the repository.
# The versions below are the minimums the code is known to work with.
numpy>=1.24
pandas>=2.0
scikit-learn>=1.4
scipy>=1.10
xgboost>=2.0
joblib>=1.3
matplotlib>=3.7
pyyaml>=6.0
pytest>=7.4
